# 🧱 Structure Material Planner — 統合版(Bedrock) 開発ロードマップ

## ✅ 完了済みの機能 (今回の追加要件を含む)
- [x] **3D形状の高度化**: 階段・ハーフ・フェンス等の専用ジオメトリ実装 (`blockshapes.js`)
- [x] **3DビューUI整理**: 右サイドパネルへの設定項目グループ化・収納
- [x] **Canva風D&D**: サイドバーへのファイルドロップによる即時インポート
- [x] **マップアート切替**: 「見た目」と「マップカラー」のプレビュー切替
- [x] **CSV/Markdown出力**: 素材リストの書き出し機能

---

## 🔥 1. システム基盤・データ管理（最優先）
- [ ] **IndexedDB への移行**
  - localStorage (5MB) からの脱却。巨大な構造物や画像データをブラウザに永続保存。
- [ ] **状態のリアルタイム連携**
  - ドット絵、3D、素材一覧のデータを同期し、タブ切替時の再計算を不要にする。

## 🧊 2. 3Dビューアの拡張機能
- [ ] **マルチモデル表示 (Multi-model)**
  - 1つのシーンに複数の `.mcstructure` を同時表示。
  - 各モデルの位置（XYZ）調整機能。
- [ ] **インポート選択 UI**
  - 読み込み時に「現在のシーンに追加（マージ）」か「新規上書き」かを選択。

## 🎨 3. 統合版仕様のマップアート・ドット絵
- [ ] **地図スケール対応と自動分割**
  - 統合版地図（128x128）をベースに、Scale 0〜4 に合わせたグリッド表示。
  - 巨大な画像を Scale に応じて 2x2, 4x4 枚などの地図用データに自動分割。
- [ ] **座標スナップと「縁」の除外**
  - 最寄りの地図の角（128の倍数）への自動吸着。
  - 地図の最外周（色がバグる縁）を自動で避けて配置するオフセット計算。

## 📂 4. UI/UX の改善
- [ ] **ホームに戻る機能の再実装・視認性向上**
  - ロゴがホームボタンを兼ねていますが、ユーザーが「消えた」と感じない明確な導線の確保。
- [ ] **準備済みチェックの構造別管理**
  - プロジェクト単位ではなく、追加した各構造ファイルごとにチェック状態を独立させる。

## 📜 5. 統合版向けコマンド・設置アシスト
- [ ] **分割 /fill コマンド生成**
  - 32,768ブロック制限を考慮した、範囲ごとのコマンド自動作成。
- [ ] **ストラクチャーブロック連携**
  - 設置ガイドとしての「サイズ」と「オフセット」の数値を自動計算・表示。
- [ ] **設置ガイドUI**
  - 複数枚に分割した際、「どの座標にどのファイルを置くか」の図解表示。

---

## 📝 開発時の技術要件 (統合版最新仕様)
- **Little-endian**: NBT解析/書き出しは常に LE 形式。
- **32,768 Limit**: `/fill` や `/clone` の一括処理上限を厳守。
- **128x128 Map**: 地図1枚の最小単位。
