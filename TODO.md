# 📌 プロジェクト・コンテキスト（AIへの最重要指示）

本プロジェクトは、Minecraft 統合版（Bedrock Edition）のサバイバル・マルチプレイ向けの高度な建築支援Webアプリケーションの開発です。
AIは以下の「特殊な前提環境」と「ツールの真の目的」を必ず理解した上で、すべての提案・実装を行ってください。

## 👑 前提条件：エンドゲーム環境
1.  **無限素材環境**: 全アイテム自動仕分け機が完備されており、素材コストは度外視。コスト削減よりも「デザインの着せ替え（リッチ化）」に価値を置く。
2.  **ホログラム環境**: Litematica形式（`.litematic`）等をゲーム内に直接投影できる環境を導入済み。「どこに置くか」のガイドはホログラムに任せる。

## 🎯 ツールの真の役割
このツールは「設計図ビューア」ではなく、**「マイクラのデータ構造をハックし、ホログラム建築の効率とマルチプレイの連携を極限まで高めるバックオフィスツール」**である。

*   **データ合体 ＆ 変換**: 複数の `.mcstructure`（現在はストラクチャーデータのみを対象）を合体させ、巨大な `.litematic`（Java版形式）として出力。
*   **データ改変による先回り支援**: ホログラムに従うだけで「湧き潰し」や「足場設置」が完了するように、出力前にストラクチャーデータを加工（ハック）する。
*   **分業の最適化**: 巨大建築をフレンドと手分けできるよう、データを部位別（外装/内装、階層、回路等）に分離・最適化する。

---

# Minecraft Structure Material Planner - ロードマップ ＆ 検討事項

## 🚀 巨大構造物の結合 ＆ Litematica エクスポート機能の実現

### 🏗️ 実装の柱
1.  **ストラクチャーの結合処理**: 複数ファイルをメモリ上でオフセット座標を適用して合体。
2.  **部位別一括置換（カスタマイズ＆プリセット）**:
    *   「屋根」「壁」「床」などの部位ごとの素材管理。
    *   **類似ブロック・サジェスト**: ブロック選択時、選択したブロックとカテゴリや質感が似ているブロック（例：板材なら他の木種、石系なら他の石材など）を一覧表示し、直感的に代案を選べる機能。
3.  **X線（透過）インスペクション ＆ レイヤー分離**:
    *   3Dビュー上で不透過ブロックを半透明化し、内部（回路や内装）を透視。
    *   「外装のみ」「内装のみ」「レッドストーンのみ」といった、カテゴリ別の分離エクスポート。
4.  **Java版スキーマ変換**: `BlockStatePalette` の構築と `BlockStates` のビットパック。
5.  **バイナリ出力**: `NBTWriter('be')` によるビッグエンディアンでの `.litematic` 生成。

---

## 🔍 特殊加工（データハック機能）

### ⛏️ くり抜き（Excavation）ガイド生成（インバートモード）
*   **空間データの反転**: 「空気」を目立つブロックに置換し、逆に「実体」を消去。
*   **用途**: ホログラムが表示されている場所を掘るだけで、設計通りの空間が確保できる。

### 🪜 足場（Scaffolding）自動生成アシスト
*   **浮遊ブロック検出**: 下に支えがないブロックの真下に、地面まで足場の柱を自動挿入。
*   **用途**: 足場も含めたホログラムを出すことで、空中作業の準備を不要にする。

### 🔦 隠し光源オート・パッチ（湧き潰し自動化）
*   **隠し場所の自動検出**: カーペット下やハーフブロック裏などをスキャン。
*   **光源置換**: 検出場所を自動で光源ブロックに置換。外観を保ったまま湧き潰しを完結させる。

---

## 📦 スマート・シュルカーパッキング（資材運搬の最適化）

### 📱 モバイル対応パッキングリスト
*   **オート・パッキング**: 全資材をシュルカー（27スロット）に効率分配。同系統素材でのまとめ。
*   **ビジュアル表示**: チェスト内の中身をテクスチャ付きで表示。
*   **階層型チェック**: アイテム個別 ➔ シュルカー全体の「完了」演出による進捗管理。

---

## 📅 次回の着手予定

### 1. コードの細分化・リファクタリング（最優先 ⭐️）
*   `app.js` 等の巨大ファイルをモジュール化。
*   データ変換ロジックを独立させ、今後の複雑なハック機能の実装を容易にする。

### 2. 結合処理 ＆ インバートモードのモックアップ作成
*   小規模データでの座標合体と、空気ブロックの置換テスト。
